#!/system/bin/sh
# Check if we have root; if not, re-run with su
if [ "$(id -u)" -ne 0 ]; then
    exec su -c "$0" "$@"
fi

am start-activity -a android.intent.action.MAIN -p com.android.shell -n com.android.shell/.BugreportWarningActivity -c org.lsposed.manager.LAUNCH_MANAGER